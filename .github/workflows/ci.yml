# CI for the locallingo gem: run the suite + lint on every supported Ruby.
#
# `bundle exec rake` is `spec + rubocop` (see Rakefile) — the same command run
# locally. SimpleCov enforces the coverage floor from within the suite, so a
# coverage regression fails this job too.
name: CI

on:
  push:
    branches: [main]
  pull_request:

# Cancel superseded runs on the same ref (a force-push / new commit to a PR).
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rake:
    name: rake (Ruby ${{ matrix.ruby }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ruby: ["3.2", "3.3", "3.4"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Ruby ${{ matrix.ruby }}
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
          bundler-cache: true

      - name: Run the suite + lint
        run: bundle exec rake
